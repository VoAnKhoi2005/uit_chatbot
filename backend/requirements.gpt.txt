# FastAPI and server
fastapi>=0.104.0
uvicorn[standard]>=0.24.0

# Pydantic for data validation
pydantic>=2.0.0
pydantic-settings>=2.2.1

# RDF/Ontology
rdflib>=6.3.0

# Vector embeddings and ML (CPU-only versions)
numpy>=1.24.0
pandas>=1.5.0

# Install PyTorch CPU-only version FIRST (before sentence-transformers)
--extra-index-url https://download.pytorch.org/whl/cpu
torch
torchvision
torchaudio

# Now install sentence-transformers (will use CPU PyTorch above)
sentence-transformers>=2.2.0

# NLP models for Vietnamese
phonlp>=0.2.0
py-vncorenlp==0.1.4

# HTTP client for Groq API
requests>=2.31.0

# OpenAI API client
openai>=1.0.0

# Environment variables
python-dotenv>=1.0.0
