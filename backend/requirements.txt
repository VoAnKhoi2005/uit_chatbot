# FastAPI and server
fastapi>=0.104.0
uvicorn[standard]>=0.24.0

# Pydantic for data validation
pydantic>=2.0.0
pydantic-settings>=2.2.1

# RDF/Ontology
rdflib>=6.3.0

# Vector embeddings and ML
# Embeddings are computed server-side via the OpenAI-protocol API
# (retrieval/text_rag/embeddings.py, default model Qwen3 Embedding 8B) - no
# local embedding model/torch needed for the live pipeline. sentence-transformers
# is only needed for the legacy, unused-by-the-live-pipeline
# retrieval.src.db.vector_db.ConceptRelationDB / ingestion tools; install it
# separately (`pip install sentence-transformers`) if you need those.
numpy>=1.24.0
faiss-cpu>=1.7.4

# HTTP client for Groq API
requests>=2.31.0

# OpenAI API client
openai>=1.0.0

# Environment variables
python-dotenv>=1.0.0

